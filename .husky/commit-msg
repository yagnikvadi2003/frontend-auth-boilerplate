# #!/bin/sh

# # Check if the commit message is empty
# if [ -z "$HUSKY_GIT_PARAMS" ]; then
#     echo "Error: Please use 'pnpm commit' instead of 'git commit -m \"\"'."
#     exit 1
# fi

# # Check if the commit message starts with 'pnpm commit'
# if ! grep -qE "^\s*pnpm\s+commit\b" "$HUSKY_GIT_PARAMS"; then
#     echo "Error: Please use 'pnpm commit' for commits."
#     exit 1
# fi

# # Continue with the commit
# exit 0


#!/bin/sh

# Check if the commit message file is empty
if [ ! -s "$1" ]; then
    echo "Error: Commit message is empty. Please provide a commit message."
    exit 1
fi

# Continue with the commit
exit 0
